"use strict";

module.exports = {
	_obm: {},
	httpServer: function() {
		var p = new $.promise();
		
		p.resolve(); // replace with real
		
		return (p);
	},
	database: function() {
		var p = new $.promise();

		p.resolve(); // replace with real
		
		return (p);
	}
};